import app from "./app";

// database config

// other services configurations error handling related to them

app.listen(8000, () => {
  console.log("Server is running on http://localhost:8000");
});
